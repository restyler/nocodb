This directory can contain custom branding assets.
Files placed here are copied into the **packages/nc-gui** folder before building a Docker image via `build-local-docker-image.sh`.
Use the same folder structure as in `packages/nc-gui` (e.g. `assets/img/brand/nocodb-full.png`, `public/favicon.ico`).
